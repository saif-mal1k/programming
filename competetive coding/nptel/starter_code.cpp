#include <bits/stdc++.h>
using namespace std;

#define ll long long int
#define vi vector<int>
#define vll vector<long long int>

int main()
{

#ifndef ONLINE_JUDGE
    freopen("in.txt", "r", stdin);
    freopen("out.txt", "w", stdout);
#endif

    std ::ios::sync_with_stdio(false);
}

// this is the starter code as introduced in week1 reversort lecture






/*

    // this is just an example code for reversort put it in main and run it

    int C;      // no of test array
    cin>> C;

    for (int j=0; j<C; j++){

        int N;      // size of each array
        cin >> N;

        vi L;       // for elements in array we are using vector of type int

        for (int i = 0; i < N; i++){

            int num;
            cin>> num;
            L.push_back(num);
            
        }
        for (auto element : L){             // to print elements in L
            cout << element <<" ";
        }
        cout<<endl;

    }


// above code assumes that you have 
------------------------
3
4
4 2 1 3
2
1 2
7
7 6 5 4 3 2 1
------------------------
this ^ data in in.txt file

*/